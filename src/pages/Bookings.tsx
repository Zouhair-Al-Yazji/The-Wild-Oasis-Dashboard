import Error from "@/ui/Error";
import Spinner from "@/ui/Spinner";

export default function Bookings() {
  return (
    <div>
      Bookings
      <Spinner />
      <Error message="ss" />
    </div>
  );
}
